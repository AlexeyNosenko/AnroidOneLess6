<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.master.anroidoneless6"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion = "14"
        android:maxSdkVersion = "21"
    />
    <application
        android:allowBackup="true"
        android:icon="@drawable/style_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".TopActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NailActivity"/>
        <activity android:name=".NailCategoryActivity"/>
        <activity android:name=".HairActivity"/>
        <activity android:name=".HairCategoryActivity"/>
    </application>

</manifest>